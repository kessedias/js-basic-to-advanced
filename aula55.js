const frutas = ['Maçã', 'Uva', 'Pêra', 'Banana', 'Ameixa', 'Abacaxi'];

for (let i in frutas){
    console.log(frutas[i]);
}

const pessoa = {
    nome: 'Zé',
    sobrenome: 'Do Gás',
    idade: 30
};

for (let chave in pessoa){
    console.log(chave);
}